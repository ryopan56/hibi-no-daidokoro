# Codex Session Log

日時: 2026-03-02 18:23
タスク: task-00 bootstrap

## 指示内容
- Docker Compose 前提で Django 5.2 + PostgreSQL の雛形作成
- Django はコンテナ内で実行
- `src/` 配下に Django プロジェクトを作成
- `.env.example` 作成、`.env` は追跡しない
- `.gitignore` 更新
- logging 設定追加、`logs/app/` に出力（Git管理しない）
- `docker compose up` で起動、`python manage.py migrate` 成功
- `http://localhost:8000/` が 200
- セッションログ保存

## Codexの応答
- Docker Compose / Dockerfile / requirements を作成
- `django-admin startproject` をコンテナ内で実行
- settings に PostgreSQL と logging を設定
- `.env.example` / `.gitignore` 作成
- `docker compose run --rm web python manage.py migrate` 実行
- `docker compose up -d` で起動

## 実施変更
- `docker-compose.yml` 追加
- `Dockerfile` 追加
- `requirements.txt` 追加
- `.env.example` 追加
- `.gitignore` 追加
- `src/` に Django プロジェクト生成
- `src/hibi_no_daidokoro/settings.py` を環境変数/DB/logging対応に更新

## 発生問題
- Docker デーモン未起動で `docker compose run` が失敗
- `curl http://localhost:8000/` が接続失敗（環境起因の可能性）

## 解決方法
- Docker 起動後に `docker compose run` を再実行
- `docker compose restart web` で再起動
- web ログで起動は確認済み
